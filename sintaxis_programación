//sintaxis de estructuras...

// while
while (condicion) {
    instrucciones;
}

// for
/*En C, la sentencia FOR se inicializa con 0 */

for (inicializacion; condicion; actualizacion) {
    instrucciones;
}
/* ejemplo:
for (i = 1; i <= 5; i = i + 1) {
  printf("%d\n", i);
}
*/

//switch
switch (variable) {
    case valor1:
        instrucciones;
        break;

    case valor2:
        instrucciones;
        break;

    default:
        instrucciones;
}

//do while
do {
    instrucciones;
} while (condicion);

//if else
if (condicion) {
    instrucciones;
} else {
    instrucciones_si_falso;
}

